@model IEnumerable<tcc_core.Models.Agendamento>

@{
    ViewData["Title"] = "Agendamentos";
}

<h1>@ViewData["Title"]</h1>
<hr />

<p>
    <a asp-action="Create" class="btn btn-success">Adicionar</a>
</p>
<table class="table">
    <thead>
        <tr class="text-center">
            <th>Responsável Interno</th>
            <th>Responsável Externo</th>
            <th> Data Inicial</th>
            <th>Data Final</th>
            <th>Qtd. Pessoas</th>
            <th>Ações</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
            <tr class="text-center">
            <td>
                @Html.DisplayFor(modelItem => item.ResponsavelInterno)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ResponsavelExterno)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DtInicial)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DtFinal)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.QtdPessoas)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-dark">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-secondary">Ver detalhes</a> |
                <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger">Excluir</a>
            </td>
        </tr>
}
    </tbody>
</table>
